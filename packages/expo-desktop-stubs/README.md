# expo-desktop-stubs

This is a native module to stub out just enough functionality for React Native Windows apps to be able to survive the side-effects from the `import { registerRootComponent } from "expo"` you inevitably run in the entrypoint for any Expo app.
