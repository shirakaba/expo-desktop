# We have set `includesGeneratedCode: false`, so intend the consumer to run the
# codegen, rather than prebuilding and distributing it from the library.
codegen

# `pnpm pack` outputs
expo-desktop-stubs-*.tgz
package
