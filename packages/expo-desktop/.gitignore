# I tend to test the CLI by creating an app under this directory.
/MyApp*

# The JS build from the TS sources (Node.js can't run TS from node_modules).
/build

# Written by scripts/build-cli.mjs for CI when npm publish --json hides lifecycle logs.
/expo-desktop-build.log
